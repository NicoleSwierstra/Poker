package Networking;

import Graphics.GraphicsTable;
import Graphics.Window;
import Graphics.GUI.GUI;

public class TableServer extends GraphicsTable {

    TableServer(Window win, GUI gui) {
        super(win, gui);
    }
    
}
