import Graphics.GraphicsGame;
import GameLogic.*;
import java.util.*;

public class Main{
  public static void main(String[] args){
    /*
    long totalTime = 0;
    Deck test = new Deck();
    byte[] num = new byte[7]; byte[] suit = new byte[7];

    for(int j = 0; j < 1000; j++){
      test.reset();
    for(int i = 0; i < 7; i++){
      List<Card> temp = test.copyAndRemove(1).get(0);
      num[i] = temp.get(0).num;
      suit[i] = temp.get(0).suit;
      temp.get(0).print();
    }
    TempHandVal eval = new TempHandVal(num, suit);
    eval.checkForStraights();
    System.out.println(Arrays.toString(eval.checkForStraights()));
    totalTime += eval.ttime;
    }
    System.out.println(totalTime);
    */
  }
}